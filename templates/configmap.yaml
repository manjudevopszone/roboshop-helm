apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.component_name }}
{{- with .Values.component_name }}
data:
  {{- toYaml . | nindent 2 }}
{{- end }}
#
#  {{- range $key, $value := .Values.configmap_data }}
#  {{ $key }}: {{ $value | quote }}
#  {{- end }}
