apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.component_name }}
spec:
  type: LoadBalancer
  selector:
    component: {{ .Values.component_name }}
  ports:
    - port: {{ .Values.service.port }} # service port
      targetPort: {{ .Values.service.container_port }}  # pod port
